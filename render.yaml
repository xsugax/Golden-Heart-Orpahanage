services:
  - type: web
    name: golden-heart-orphanage
    runtime: docker
    region: oregon
    plan: free
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
